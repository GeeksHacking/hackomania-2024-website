---
interface Props {
  title: string
}

import Navbar from '../components/Navbar.astro'

const { title } = Astro.props
const navItems = ['registration', 'about', 'sponsors', 'prizes', 'album', 'venue', 'team', 'contact']

---

<!doctype html>
<html lang="en">
  <head>
    <script type="text/javascript">
      ;(function (c, l, a, r, i, t, y) {
        c[a] =
          c[a] ||
          function () {
            ;(c[a].q = c[a].q || []).push(arguments)
          }
        t = l.createElement(r)
        t.async = 1
        t.src = 'https://www.clarity.ms/tag/' + i
        y = l.getElementsByTagName(r)[0]
        y.parentNode.insertBefore(t, y)
      })(window, document, 'clarity', 'script', 'k9o41x2ryc')
    </script>

    <meta charset="UTF-8" />
    <meta name="description" content="HackOMania 2024" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <Navbar navItems={navItems} />
  <body>
    <div class="px-6 bg-app-background">
      <slot />
    </div>
  </body>
</html>

<style is:global>
  @font-face {
    font-family: 'Cascadia Code';
    src: url('/fonts/CascadiaCode.woff2') format('woff');
  }
</style>
